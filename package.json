{
  "name": "motionpitch",
  "version": "0.0.0",
  "private": true,
  "description": "Type a business in one sentence, get a one-page landing site whose hero is a custom-animated SVG scene.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "check": "bun run scripts/check.ts",
    "check:agents": "bun run scripts/check.ts --only=presence,freshness",
    "check:fs": "bun run scripts/check.ts --only=forbidden",
    "typecheck": "bunx tsc --noEmit",
    "agents:stamp": "bun run scripts/stamp.ts",
    "agents:stamp-all": "bun run scripts/stamp.ts --all",
    "agents:stale": "bun run scripts/check.ts --only=freshness --verbose",
    "wt:add": "bun run scripts/worktree.ts add",
    "wt:list": "bun run scripts/worktree.ts list",
    "wt:remove": "bun run scripts/worktree.ts remove",
    "prepare": "bunx lefthook install || true"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/bun": "latest",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "lefthook": "^1.7.0",
    "postcss": "^8",
    "tailwindcss": "^4",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "framer-motion": "^12",
    "next": "^15",
    "react": "^19",
    "react-dom": "^19"
  }
}
